{
  "error_code": "mixed_schema_format",
  "message_contains": [
    "mixed",
    "group"
  ],
  "notes": "schema_version 1 (or missing) must decode as legacy wire model; any Target.group field presence is rejected at load boundary rather than silently dropped"
}
